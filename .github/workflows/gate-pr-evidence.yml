name: 🚪 PR Evidence Gate - 強制執行證據驗證

on:
  pull_request:
    branches: [main, master]
    types: [opened, edited, synchronize, reopened]

env:
  AGENT_CONTRACT_ENFORCEMENT: true
  EVIDENCE_REQUIRED: true
  STRICT_MODE: true

jobs:
  validate-pr-evidence:
    name: 🔍 驗證 PR 證據完整性
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read
    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: 驗證四大核心證據
        id: validation
        uses: actions/github-script@v7
        with:
          script: |
            const { validate } = require('./.github/scripts/validate-pr-evidence');
            validate({ context, core });

      - name: 📊 驗證結果輸出
        if: always()
        run: |
          echo "## 🚨 PR 驗證結果" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**驗證狀態:** ${{ steps.validation.outputs.status }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          if [ "${{ steps.validation.outputs.status }}" = "FAIL" ]; then
            echo "### ❌ 驗證失敗項目:" >> $GITHUB_STEP_SUMMARY
            echo "${{ steps.validation.outputs.missing-items }}" | tr ',' '\n' | sed 's/^/- /' >> $GITHUB_STEP_SUMMARY
          else
            echo "### ✅ 驗證通過" >> $GITHUB_STEP_SUMMARY
            echo "所有四大核心證據都已提供且格式正確。" >> $GITHUB_STEP_SUMMARY
          fi
