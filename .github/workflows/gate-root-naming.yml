name: 🚪 Root Naming Policy Gate

on:
  pull_request:
    types: [opened, edited, synchronize, reopened, ready_for_review]
    paths:
      - 'root/**'

permissions:
  contents: read
  pull-requests: write

jobs:
  validate_root_naming:
    name: 🔍 驗證 root/ 命名規範
    runs-on: ubuntu-latest

    steps:
      - name: 📥 檢出代碼
        uses: actions/checkout@v4

      - name: 🔍 檢查 root/ 變更檔案
        uses: actions/github-script@v7
        with:
          script: |
            const owner = context.repo.owner;
            const repo = context.repo.repo;
            const prNumber = context.payload.pull_request.number;

            const files = await github.paginate(
              github.rest.pulls.listFiles,
              { owner, repo, pull_number: prNumber, per_page: 100 }
            );

            const changedFiles = files.map(f => f.filename);
            const rootChanges = changedFiles.filter(p => p.startsWith("root/"));

            if (rootChanges.length === 0) {
              core.info("✅ 本 PR 未變更 root/ 目錄，跳過命名規範檢查");
              return;
            }

            core.info(`📋 檢測到 ${rootChanges.length} 個 root/ 變更檔案`);

            // 允許的檔案模式
            const allowedPatterns = [
              /^root\/README\.md$/,
              /^root\/AGENT_DELIVERY_CONTRACT\.md$/,
              /^root\/VALIDATION_CHECKLIST\.md$/,
              /^root\/root\.config\.yaml$/,
              /^root\/root\.governance\.yaml$/,
              /^root\/root\.modules\.yaml$/,
              /^root\/root\.super-execution\.yaml$/,
              /^root\/root\.trust\.yaml$/,
              /^root\/root\.provenance\.yaml$/,
              /^root\/root\.integrity\.yaml$/,
              /^root\/root\.bootstrap\.yaml$/,
              /^root\/root\.naming-policy\.yaml$/,
              /^root\/root\.devices\.map$/,
              /^root\/root\.fs\.map$/,
              /^root\/root\.kernel\.map$/,
              /^root\/root\.env\.sh$/,
              /^root\/gates\.map\.yaml$/,
              /^root\/init\.d\/.*$/,
              /^root\/jobs\/.*$/,
            ];

            // 禁止的模式
            const denyPatterns = [
              { pattern: /[A-Z]/, reason: "禁止大寫字母" },
              { pattern: /\s/, reason: "禁止空白字元" },
              { pattern: /\.yml$/, reason: "統一使用 .yaml（不用 .yml）" },
            ];

            const violations = [];

            for (const filepath of rootChanges) {
              // 檢查禁止模式
              for (const deny of denyPatterns) {
                if (deny.pattern.test(filepath)) {
                  violations.push({
                    file: filepath,
                    reason: deny.reason,
                    severity: "error"
                  });
                }
              }

              // 檢查是否符合允許模式
              const isAllowed = allowedPatterns.some(pattern => pattern.test(filepath));
              if (!isAllowed) {
                violations.push({
                  file: filepath,
                  reason: "不符合 root/ 命名規範 allowlist",
                  severity: "error"
                });
              }
            }

            // 生成報告
            let report = "## 🔍 Root 命名規範檢查報告\n\n";
            
            if (violations.length === 0) {
              report += "### ✅ 檢查通過\n\n";
              report += `所有 ${rootChanges.length} 個檔案都符合命名規範。\n\n`;
              report += "**變更檔案清單:**\n";
              rootChanges.forEach(f => {
                report += `- ✅ \`${f}\`\n`;
              });
              
              core.info(report);
              
              await github.rest.issues.createComment({
                owner,
                repo,
                issue_number: prNumber,
                body: report
              });
              
              return;
            }

            // 有違規
            report += "### ❌ 檢查失敗\n\n";
            report += `發現 ${violations.length} 個違規項目：\n\n`;
            
            violations.forEach((v, i) => {
              report += `${i + 1}. **\`${v.file}\`**\n`;
              report += `   - ❌ ${v.reason}\n\n`;
            });

            report += "\n---\n\n";
            report += "### 📋 Root 命名規範要求\n\n";
            report += "**允許的檔案模式:**\n";
            report += "- `root/README.md`\n";
            report += "- `root/AGENT_DELIVERY_CONTRACT.md`\n";
            report += "- `root/root.<category>.yaml` (例如: `root.config.yaml`)\n";
            report += "- `root/root.<category>.map` (例如: `root.devices.map`)\n";
            report += "- `root/init.d/**`\n";
            report += "- `root/jobs/**`\n\n";
            
            report += "**禁止的模式:**\n";
            report += "- ❌ 大寫字母\n";
            report += "- ❌ 空白字元\n";
            report += "- ❌ 使用 `.yml` (請統一使用 `.yaml`)\n\n";
            
            report += "**修正方式:**\n";
            report += "1. 重命名違規檔案以符合規範\n";
            report += "2. 更新 PR 並重新提交\n";
            report += "3. 若需新增新的檔案類型，請先更新 `root/root.naming-policy.yaml`\n";

            await github.rest.issues.createComment({
              owner,
              repo,
              issue_number: prNumber,
              body: report
            });

            core.setFailed(
              `Root 命名規範檢查失敗：發現 ${violations.length} 個違規項目。請查看 PR 評論了解詳情。`
            );

      - name: 📊 輸出檢查摘要
        if: always()
        run: |
          echo "## 🚪 Root Naming Policy Gate" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "本次檢查針對 root/ 目錄的命名規範進行驗證。" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "詳細結果請查看 PR 評論。" >> $GITHUB_STEP_SUMMARY