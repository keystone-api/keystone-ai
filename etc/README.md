# Controlplane-managed configuration

This directory intentionally contains no configuration. All root-level governance and configuration lives under `controlplane/baseline/config`. Refer to those sources for the authoritative versions.
