apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: quantumflow-data
  namespace: quantumflow
  labels:
    app: quantumflow
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: standard

