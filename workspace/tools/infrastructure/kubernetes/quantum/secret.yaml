apiVersion: v1
kind: Secret
metadata:
  name: quantumflow-secrets
  namespace: quantumflow
  labels:
    app: quantumflow
type: Opaque
stringData:
  # Quantum Backend API Keys (set these in your environment)
  # CIRQ_API_KEY: ""
  # QISKIT_API_KEY: ""
  # PENNYLANE_API_KEY: ""
  SECRET_KEY: "change-me-in-production-use-strong-random-key"
  ALLOWED_ORIGINS: "http://localhost:3000,https://yourdomain.com"

